import { RemoveApiPipe } from './remove-api.pipe';

describe('RemoveApiPipe', () => {
  it('create an instance', () => {
    const pipe = new RemoveApiPipe();
    expect(pipe).toBeTruthy();
  });
});
