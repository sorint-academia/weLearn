import { Slash2underscorePipe } from './slash2underscore.pipe';

describe('Slash2underscorePipe', () => {
  it('create an instance', () => {
    const pipe = new Slash2underscorePipe();
    expect(pipe).toBeTruthy();
  });
});
